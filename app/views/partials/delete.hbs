
<br>
        <div class="uk-card uk-card-primary uk-card-body">
            <h3 class="uk-card-title">Delete an Image</h3>
            <form class="uk-form-horizontal" action="/deleteimage/{id}" method="GET" enctype="multipart/form-data">
                    <div class="uk-card uk-card-default uk-card-small uk-text-center uk-text-baseline uk-animation-scale-up">
                        <div class="uk-card-media-top">
                            <img src="{{url}}">
                        </div>
                        <div>
                            <div class="uk-card-footer">
                                <p> {{public_id}}</p>
                                <a href="/deleteimage/{{public_id}}" uk-icon="icon: trash"></a>
                            </div>
                        </div>
                    </div>
                </div>